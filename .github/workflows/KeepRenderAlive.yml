name: Keep Render App Alive

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -f https://kylekrane.in/emptyclassroomfinder/health
